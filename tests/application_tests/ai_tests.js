'use strict';

describe('Tests for AI decision making', function() {
    describe('testing AI defense capability', function() {
        var data = {};
        var data.gameFields = testData.gameFields();
        var data = 

        var data.aiPlayer = testData.aiPlayer(data.gameFields);

        test('AI is defending itself in case when opponent has one way of winning', function() {
            data.
        });
    })
});